{% extends "UsersAdminuserBundle::layoutadmin.html.twig" %}
{% block meta %}
{{ parent() }}
<meta name="keywords" content="{{ site }}, Business, Innovation,Administration"/>
<meta name="author" content="Noel Kenfack"/>
<meta name="description" content="{{ site }} | Inscription | Cameroun | {{ metier }}" />
{% endblock %}
{% block title %}
{{ parent() }} | Accueil Admin
{% endblock %}

{% block userblog_body %}
<!--content-->
		<div class="product" style="padding-top: 135px; margin-bottom: 20px;">
			<div class="container" style="padding: 0px;">
			<div class="col-lg-12 animecourant-panel">
			</div>
				<div class="col-md-4 product-bottom">
				{{ render(controller("UsersAdminuserBundle:Accueil:menuadmin")) }}
				<!---->
				</div>
				<div class="col-md-8">
				<div class="panel panel-widget" style="border: 1px solid #ddd; margin-bottom: 7px;">
						<h2 style="margin: 5px; background: #f7f7f7; ">Gagnants 3Rencontres le ''{{ souscategorie.datetext }}'' </h2>	
				</div>
				<h4 class="title">
					{% for infos in app.session.flashbag.get('information') %}
					{% if loop.first %}
					<div class="alert alert-warning">{{ infos|raw }}</div>
					{% endif %}
					{% endfor %}
				</h4>
				<div class="row">
					{% for panier in panier_trois %}
					<div class="col-md-6">
					<div style="border: 1px solid #ddd; width: 100%;">
					<div class="message-top" style="border-bottom: 1px solid #ddd;">
					<div class="message-left">
					<h2 style="color: #fff;">Ticket N° {{ panier.numfacture }}</h2>
					</div>
					<div class="message-right">
					<i class="glyphicon glyphicon-stop" aria-hidden="true"></i>
					</div>
					<div class="clearfix"></div>
					</div>
					{% for propan in panier.produitpaniers %}
					{% set produit = propan.produit %}
					<div class="message-bottom" style="border-bottom: 1px solid #ddd;">
						<div class="message1-left">
							<h4>{% if propan.quantite == 0 %}{{ produit.equipedom.nom }} # {{ produit.equipeext.nom }}{% else %}{% if propan.quantite == 1 %}<a href="#!" class="btn-primary">{{ produit.equipedom.nom }}</a> # <a href="#!" class="btn-danger">{{ produit.equipeext.nom }}</a>{% else %}<a href="#!" class="btn-danger">{{ produit.equipedom.nom }}</a> # <a href="#!" class="btn-primary">{{ produit.equipeext.nom }}</a>{% endif %}{% endif %}  {% if produit.updatescore == true %}{{ produit.butdom }}#{{ produit.butext }}{% else %}{{ produit.heure }}:{{ produit.minute }}{% endif %}</h4>
							<p><strong>Paris:</strong> {% if propan.quantite == 0 %}Match null{% else %}{% if propan.quantite == 1 %}Victoire à domicile du {{ produit.equipedom.nom }}{% else %}Victoire à l'extérieure du {{ produit.equipeext.nom }}{% endif %}{% endif %}</p>
						</div>
						<div class="clearfix"></div>
					</div>
					{% endfor %}
					
					<div class="message-bottom" style="border-bottom: 1px solid #ddd;">
						<div class="message1-left">
							<h4><img src="{% if panier.user.imgprofil != null %} {{ asset(panier.user.imgprofil.webpath) }} {% else %} {{ asset('template/images/co.png') }} {% endif %}" alt="{{ panier.user.name(20) }}" style="width: 30px; height: 30px;"/> <a href="{{ path('users_user_user_accueil', {'id': panier.user.id }) }}">{{ panier.user.name(15) }} ::{% set montant = panier.nbticket * panier.montant %}{{ montant }}{{ devise }}::{{ panier.gain }}{{ devise }}::</a></h4>
							<p>Email: {{ panier.user.username }} - Tel: {{ panier.user.tel }}</p>
						</div>
						<div class="clearfix"></div>
					</div>
						
					</div>
					</div>
					{% else %}
					
					<div class="col-lg-12 text-left">
					<div class="panel panel-widget alert alert-warning" style="border-radius: 0px;">
					<span class=" fa fa-exclamation-circle"></span> Aucun ticket n'a gagner trois rencontres pour la liste du {{ souscategorie.datetext }} sur {{ site }}. <span class="fa fa-warning pull-right"></span>
					</div>
					<div class="clearfix"></div>
					</div>
					
					{% endfor %}
				   <div class="clearfix"> </div>
				</div>
				
				<div class="panel panel-widget" style="border: 1px solid #ddd; margin-bottom: 7px; margin-top: 7px;">
						<h2 style="margin: 5px; background: #f7f7f7; ">Gagnants 4Rencontres le ''{{ souscategorie.datetext }}'' </h2>	
				</div>
				
				<div class="row">
					{% for panier in panier_quatre %}
					<div class="col-md-6">
					<div style="border: 1px solid #ddd; width: 100%;">
					<div class="message-top" style="border-bottom: 1px solid #ddd;">
					<div class="message-left">
					<h2 style="color: #fff;">Ticket N° {{ panier.numfacture }}</h2>
					</div>
					<div class="message-right">
					<i class="glyphicon glyphicon-stop" aria-hidden="true"></i>
					</div>
					<div class="clearfix"></div>
					</div>
					{% for propan in panier.produitpaniers %}
					{% set produit = propan.produit %}
					<div class="message-bottom" style="border-bottom: 1px solid #ddd;">
						<div class="message1-left">
							<h4>{% if propan.quantite == 0 %}{{ produit.equipedom.nom }} # {{ produit.equipeext.nom }}{% else %}{% if propan.quantite == 1 %}<a href="#!" class="btn-primary">{{ produit.equipedom.nom }}</a> # <a href="#!" class="btn-danger">{{ produit.equipeext.nom }}</a>{% else %}<a href="#!" class="btn-danger">{{ produit.equipedom.nom }}</a> # <a href="#!" class="btn-primary">{{ produit.equipeext.nom }}</a>{% endif %}{% endif %}  {% if produit.updatescore == true %}{{ produit.butdom }}#{{ produit.butext }}{% else %}{{ produit.heure }}:{{ produit.minute }}{% endif %}</h4>
							<p><strong>Paris:</strong> {% if propan.quantite == 0 %}Match null{% else %}{% if propan.quantite == 1 %}Victoire à domicile du {{ produit.equipedom.nom }}{% else %}Victoire à l'extérieure du {{ produit.equipeext.nom }}{% endif %}{% endif %}</p>
						</div>
						<div class="clearfix"></div>
					</div>
					{% endfor %}
					
					<div class="message-bottom" style="border-bottom: 1px solid #ddd;">
						<div class="message1-left">
							<h4><img src="{% if panier.user.imgprofil != null %} {{ asset(panier.user.imgprofil.webpath) }} {% else %} {{ asset('template/images/co.png') }} {% endif %}" alt="{{ panier.user.name(20) }}" style="width: 30px; height: 30px;"/> <a href="{{ path('users_user_user_accueil', {'id': panier.user.id }) }}">{{ panier.user.name(15) }} ::{% set montant = panier.nbticket * panier.montant %}{{ montant }}{{ devise }}::{{ panier.gain }}{{ devise }}::</a></h4>
							<p>Email: {{ panier.user.username }} - Tel: {{ panier.user.tel }}</p>
							<p>OrangeMoney: {{ panier.user.orangemoney }} - MtnMobileMoney: {{ panier.user.mtnmobile }}</p>
							<h4>Solde Gain: {{ panier.user.soldegain }}{{ devise }} <a href="#!" class="btn btn-primary solder-gain-{{ panier.user.id }}">Payer</a> </h4>
						</div>
						<div class="clearfix"></div>
					</div>
						
					</div>
					<form action="{{ path('users_adminuser_solder_gain_courant_user', {'id': souscategorie.id, 'user': panier.user.id }) }}" method="post" class="form-solde form-solde-{{ panier.user.id }}" style="display: none;">
					<input type="number" class="form-control" name="montant" style="margin-top: 15px;" placeholder="Solder le gain de {{ panier.user.name(30) }}" required="">
					<button class="btn btn-primary btn-block" style="margin-top: 15px;">Envoyer <span class="glyphicon glyphicon-send"></span></button>
					</form>
					<script type="text/javascript">
					$('.solder-gain-{{ panier.user.id }}').click(function(){
						$('.form-solde').hide();
						$('.form-solde-{{ panier.user.id }}').show('slow');
					});
					</script>
					</div>
					{% else %}
					
					<div class="col-lg-12 text-left">
					<div class="panel panel-widget alert alert-warning" style="border-radius: 0px;">
					<span class=" fa fa-exclamation-circle"></span> Aucun ticket n'a gagner quatre rencontres pour la liste du {{ souscategorie.datetext }} sur {{ site }}. <span class="fa fa-warning pull-right"></span>
					</div>
					<div class="clearfix"></div>
					</div>
					
					{% endfor %}
				   <div class="clearfix"> </div>
				</div>
				
				<div class="panel panel-widget" style="border: 1px solid #ddd; margin-bottom: 7px; margin-top: 7px;">
						<h2 style="margin: 5px; background: #f7f7f7; ">Gagnants 5Rencontres le ''{{ souscategorie.datetext }}'' </h2>	
				</div>
				
				<div class="row">
					{% for panier in panier_cinq %}
					<div class="col-md-6">
					<div style="border: 1px solid #ddd; width: 100%;">
					<div class="message-top" style="border-bottom: 1px solid #ddd;">
					<div class="message-left">
					<h2 style="color: #fff;">Ticket N° {{ panier.numfacture }}</h2>
					</div>
					<div class="message-right">
					<i class="glyphicon glyphicon-stop" aria-hidden="true"></i>
					</div>
					<div class="clearfix"></div>
					</div>
					{% for propan in panier.produitpaniers %}
					{% set produit = propan.produit %}
					<div class="message-bottom" style="border-bottom: 1px solid #ddd;">
						<div class="message1-left">
							<h4>{% if propan.quantite == 0 %}{{ produit.equipedom.nom }} # {{ produit.equipeext.nom }}{% else %}{% if propan.quantite == 1 %}<a href="#!" class="btn-primary">{{ produit.equipedom.nom }}</a> # <a href="#!" class="btn-danger">{{ produit.equipeext.nom }}</a>{% else %}<a href="#!" class="btn-danger">{{ produit.equipedom.nom }}</a> # <a href="#!" class="btn-primary">{{ produit.equipeext.nom }}</a>{% endif %}{% endif %}  {% if produit.updatescore == true %}{{ produit.butdom }}#{{ produit.butext }}{% else %}{{ produit.heure }}:{{ produit.minute }}{% endif %}</h4>
							<p><strong>Paris:</strong> {% if propan.quantite == 0 %}Match null{% else %}{% if propan.quantite == 1 %}Victoire à domicile du {{ produit.equipedom.nom }}{% else %}Victoire à l'extérieure du {{ produit.equipeext.nom }}{% endif %}{% endif %}</p>
						</div>
						<div class="clearfix"></div>
					</div>
					{% endfor %}
					
					<div class="message-bottom" style="border-bottom: 1px solid #ddd;">
						<div class="message1-left">
							<h4><img src="{% if panier.user.imgprofil != null %} {{ asset(panier.user.imgprofil.webpath) }} {% else %} {{ asset('template/images/co.png') }} {% endif %}" alt="{{ panier.user.name(20) }}" style="width: 30px; height: 30px;"/> <a href="{{ path('users_user_user_accueil', {'id': panier.user.id }) }}">{{ panier.user.name(15) }} ::{% set montant = panier.nbticket * panier.montant %}{{ montant }}{{ devise }}::{{ panier.gain }}{{ devise }}::</a></h4>
							<p>Email: {{ panier.user.username }} - Tel: {{ panier.user.tel }}</p>
						</div>
						<div class="clearfix"></div>
					</div>
						
					</div>
					</div>
					{% else %}
					
					<div class="col-lg-12 text-left">
					<div class="panel panel-widget alert alert-warning" style="border-radius: 0px;">
					<span class=" fa fa-exclamation-circle"></span> Aucun ticket n'a gagner cinq rencontres pour la liste du {{ souscategorie.datetext }} sur {{ site }}. <span class="fa fa-warning pull-right"></span>
					</div>
					<div class="clearfix"></div>
					</div>
					
					{% endfor %}
				   <div class="clearfix"> </div>
				</div>
				
				</div>
			</div class="clearfix"></div>
			</div>

<script src="{{ asset('mcs/js/onvisible.js') }}" type="text/javascript"></script>
<!-- /js files -->

<script>

{% for infos in app.session.flashbag.get('information') %}
{% if loop.first %}	
$('.infos-action-effectuer').html('{{ infos }}');
$('#infos-action-effectuer').modal('show');
{% endif %}
{% endfor %}

var bouge = 1;
var dimension = 200;
function activateelement()
{
var visibility = visibleElement('.animecourant-panel');

if(visibility && bouge == 0){
	bouge = 1;
	$('.toutleblock').css('position','relative');
	$('.toutleblock').css('bottom','0px');
	$('.toutleblock').css('margin-bottom','0px');
}
}

function stopelement()
{
var visibility = visibleElement('.stop-courant-panel');
if(visibility && bouge == 1){
	if(visibleElement('.animecourant-panel') && visibleElement('.stop-courant-panel'))
	{
	
	}else{
	bouge = 0;
	$('.toutleblock').css('position','fixed');
	$('.toutleblock').css('width',dimension+'px');
	$('.toutleblock').css('bottom','7px');
	$('.toutleblock').css('margin-bottom','165px');
	}
}
}

var idStopelement = 0;
var idActivateelement = 0;
function controlScroll()
{
	var largeur = ($(window).width());
	dimension = $('.toutleblock').width();
	if (largeur >= 768)
	{
	   /*
		if(visibleElement('.animecourant-panel') && visibleElement('.stop-courant-panel'))
		{
		
		$('.toutleblock').css('position','fixed');
		$('.toutleblock').css('width',dimension+'px'); 
		}else{ 
		idStopelement = window.setInterval(function() { stopelement(); }, 100);
		idActivateelement = window.setInterval(function() { activateelement(); }, 100);
		} */
		idStopelement = window.setInterval(function() { stopelement(); }, 100);
		idActivateelement = window.setInterval(function() { activateelement(); }, 100);
	}
}
controlScroll();

</script>			
{% endblock %}

{% block javascripttemplate %}
$('.active').removeClass('active');
$('.categorie-produit').addClass('position-user');
{% endblock %}