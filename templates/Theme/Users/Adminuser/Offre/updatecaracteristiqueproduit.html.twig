<form action="{{ path('users_adminuser_update_caracteristique_produit', {'idproduit': produit.id, 'idoffre': caracteristique.id }) }}" method="post" style="margin-top: 15px;">
    <div class="form-group">
        <div class="form-control">{{ produit.nom }}</div>
    </div>
    <div class="form-group">
    <div class="form-control">{{ caracteristique.nom }}</div>
    </div>
    
    <div class="form-group">
        <label for="valeur_caract">Ajouter une valeur</label>
        <input id="valeur_caract" type="text" class="form-control" name="valeur" value="{{ caracteristiqueplan.valeur }}"/>
    </div>

    <div class="form-group">
        <input id="valeur_caract_isChecked" type="checkbox" name="isChecked" {% if caracteristiqueplan.isChecked == true %}checked{% endif %}/>
        <label for="valeur_caract_isChecked">Allouer ce paramètre au produit</label>
    </div>
    
    <div class="form-group">
        <div style="margin-top: 15px;">
            <button class="btn btn-primary btn-lg">Enregistrer</button>
        </div>
    </div>
    <div class="clearfix"></div>
</form>	